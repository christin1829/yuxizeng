```
data source :  https://data.gov.hk/tc-data/dataset/hk-hko-rss-latest-one-minute-mean-air-temp
```

```
Using Anaconda, I deployed the environment and uploaded it to Anaconda Cloud to enable usage across different computers. I also learned how to extract locations and temperatures using zip files.
```

```
tips:
If I encounter issues with fonts not displaying, I need to set the appropriate local font based on my operating system. For example, in Windows, I can set `matplotlib.rcParams['font.family']` to "Microsoft YaHei"; in macOS, I should replace it with "PingFang" or another suitable font.
```
```
pip install -r requirements.txt
```